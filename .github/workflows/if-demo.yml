name: If demo for github actions
on:
  push:
   branches:
     - main
     - develop
jobs:
  demo:
     runs-on: ubuntu-22.04
     steps:
       - name: Run if branch is main
         if: github.ref == "refs/heads/main"
         run: echo "Running for main branch"
       - name: Run this step
         run: echo "Running this step"
