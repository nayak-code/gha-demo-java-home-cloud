name: Env demo pipeline
on: workflow_dispatch
env:
  APP_ENV: "DEV"
jobs:
  env_demo:
     env:
      APP_ENV: "Test"
     runs-on: ubuntu-latest
     steps:
       - name: Connecting to environments
         env:
           APP_ENV: "PROD"
         run: echo "This app is for $APP_ENV envoronment"
